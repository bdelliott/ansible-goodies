---
# common vars
local_user: bde
local_home: "/Users/{{ local_user }}"

username: "{{ local_user }}"

# key stuff
keyfile: id_dsa
privkeyfile: "{{ local_home }}/.ssh/{{ keyfile }}"
pubkeyfile: "{{ privkeyfile }}.pub"
pubkey: "{{ lookup('file', '/Users/bde/.ssh/id_dsa.pub') }} "

# git config
git_name: "Brian Elliott"
git_email: "bdelliott@gmail.com"
